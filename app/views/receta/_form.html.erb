<%= form_with(model: recetum) do |form| %>
  <% if recetum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recetum.errors.count, "error") %> prohibited this recetum from being saved:</h2>

      <ul>
        <% recetum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Id_receta %>
    <%= form.number_field :Id_receta %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
